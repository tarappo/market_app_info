name: "integration-test"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: AppStore Information
        id: appstore_information
        uses: ./
        with:
          bundle_identifier: "com.facebook.Facebook"
  result:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Ouputs
        run: |
          echo ${{ needs.test.outputs.app_name }}
          echo ${{ needs.test.outputs.release_date }}
          echo ${{ needs.test.outputs.version }}
